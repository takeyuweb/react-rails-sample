<%= form_for(@entry) do |f| %>
    <% if @entry.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

            <ul>
                <% @entry.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <%= react_component 'EntryForm',
                        {
                                initialState: get_state(@entry),
                                categories: Category.all.map { |c| get_state(c) }
                        },
                        static: true %>

    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>
